{
  "name": "sud-toolkit",
  "version": "0.3.1",
  "private": true,
  "description": "Clinical decision support PWA for alcohol and other drug withdrawal. The app itself ships no dependencies; this file exists so the clinical data modules can be unit tested.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
